allowBuilds:
  better-sqlite3: true
  esbuild: false

catalog:
  eslint: ~10.9.1

  # vite-plus ecosystem packages only; Nuxt must keep resolving upstream `vite`
  vite-plus: ~0.3.0

shellEmulator: true

trustPolicy: no-downgrade

trustPolicyIgnoreAfter: 10080
